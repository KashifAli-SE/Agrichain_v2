# Copy this to .env.local for local dev
# On Netlify: add these in Site Settings → Environment Variables

# Optional: override the RPC URL (defaults to MetaMask's injected provider)
NEXT_PUBLIC_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY_HERE
